# Optional overrides (all have defaults); .env is gitignored.
# Edge TTS speaking rate for narration. Natural = +0%, slightly faster = +4%.
TTS_RATE=+4%

# How many fetched candidates to dump for manual review (npm run fetch).
CANDIDATES=40
